var globalVariable = "value"; //don't ever use var because it is global and has different scope
// totalGlobal = "NEVER do this"; don't do this too
let banana = "hi monkey";
banana = "lol";
let monke2; //we can do that
//const monke3; //we can't do that
//const  top level object can't be overwritten, but we can overwrite protperties, if we have object for example.
console.log(banana);
